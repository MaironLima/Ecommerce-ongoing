// import { Router } from 'express';

// const checkoutRoutes: Router = Router();

// checkoutRoutes.post('/create-intent', checkoutCreateController);
// checkoutRoutes.post('/confirm', checkoutConfirmController);
